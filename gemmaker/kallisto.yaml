apiVersion: v1
kind: Pod
metadata:
  name: kallisto-cole
  labels:
    app: kallisto-cole
spec:
  containers:
  - name: kallisto-cole
    imagePullPolicy: Always
    image: systemsgenetics/gemmaker
    command: [ "/bin/sh" ]
    args: [ "-c", "cd /workspace/gemmaker/input && kallisto index -i /workspace/gemmaker/Arabidopsis_thaliana.TAIR10.kallisto.indexed Arabidopsis_thaliana.TAIR10.cdna.all.fa.gz" ]
    resources:
      requests:
        cpu: 1
        memory: 4Gi
      limits:
        cpu: 1
        memory: 4Gi
    volumeMounts:
    - name: gm-volume
      mountPath: /workspace
  restartPolicy: Always
  volumes:
    - name: gm-volume
      persistentVolumeClaim:
        claimName: pvc-cole # Enter valid PVC
